#!/usr/bin/env bash
# Convenience alias — same as bin/offline-video-scribe (see ~/.local/bin/ovs symlink after install).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec "$ROOT/bin/offline-video-scribe" "$@"
