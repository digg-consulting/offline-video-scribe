#!/bin/sh
# Strip Cursor co-author trailer if the editor adds it.
# Enable: git config core.hooksPath scripts/git-hooks
grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1" >"$1.tmp" && mv "$1.tmp" "$1"
