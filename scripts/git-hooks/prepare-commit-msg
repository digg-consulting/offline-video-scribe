#!/bin/sh
# Remove AI tool co-author trailers (may be added via --trailer or message editing).
tmp="${1}.ovs"
grep -viE '^co-authored-by: (cursor|github-copilot|copilot)\b' "$1" >"$tmp" 2>/dev/null || cp "$1" "$tmp"
mv "$tmp" "$1"
